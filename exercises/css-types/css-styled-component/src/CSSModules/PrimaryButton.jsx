import primaryButtonStyles from "./p-btn.module.css"

export default function Button() {
    return (
        <button className={primaryButtonStyles.primaryButton} type="button">
            oi
        </button>
    )
}